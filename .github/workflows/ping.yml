name: Keep Render Service Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # alle 10 Minuten (UTC)
  workflow_dispatch:         # optional: manuell starten möglich

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call Render Health Endpoint
        run: |
          curl -s https://pdf-service-1.onrender.com/health || true
